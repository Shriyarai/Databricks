name: Run a notebook in databricks on PRs

on:
 pull_request:

jobs:
 run-databricks-notebook:
   runs-on: ubuntu-latest
   steps:
     - name: Checkout repo
       uses: actions/checkout@v2
     - name: Run a databricks notebook
       uses: databricks/run-notebook@v0
       with:
         local-notebook-path: Workspace/Users/srai5@statestreet.com/01 - Introduction to Azure Databricks/Getting Started with Azure Databricks
         databricks-host: https://adb-7756692533412422.2.azuredatabricks.net
         databricks-token: dapi43253d9b007bb3ed010b79078008cf32-3
         git-commit: ${{ github.event.pull_request.head.sha }}
         new-cluster-json: >
           {
             "num_workers": 1,
             "spark_version": "12.0.x-cpu-ml-scala2.12",
             "node_type_id": "Standard_DS3_v2"
           }